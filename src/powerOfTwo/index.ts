/**
 * Given a list of numbers it should return a new list of numbers that are to the power of 2.
 * @param {number[]} Array
 * @returns {number[]} Returned powered array
 */
const powerOfTwo = (arr: number[]): number[] => arr.map((el) => el * el)

export default powerOfTwo
